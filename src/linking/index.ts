const config = {
    screens: {
        //   Home: {
        //     screens: {
        //       LikeScreen: "like",
        //       ChatScreen: "chat",
        //       HomeScreen: "home",
        //     },
        //   },
    },
};
const linking = {
    // prefixes: [""],
    // prefixes: ["app://fire"],
    config,
};

export default linking;

  // npx uri-scheme open https://fireindia.page.link/home/chat --android
  // npx uri-scheme open fire://app/home --android
  // npx uri-scheme open fire://app/home/like --android